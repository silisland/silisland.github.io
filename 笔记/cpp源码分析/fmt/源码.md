#fmt

入门：从 fmt/core.h 或 fmt/base.h 开始，掌握 fmt::format() 和 fmt::print() 基础用法
进阶：学习 fmt::formatter 特化，实现自定义类型的格式化
性能优化：使用 FMT_COMPILE 宏进行编译期格式化，或采用 fmt::format_to 避免临时字符串分配


[[base.h]] 

# 结构

```python
fmt/                                     ← 项目根目录
├── CMakeLists.txt                       ← CMake 构建主配置文件，支持静态库/共享库/头文件模式构建 [[77]]
├── include/
│   └── fmt/                             ← 所有公共头文件（安装后位于系统 include 目录）
│       ├── base.h                      ← 基础 API：提供最小依赖的格式化功能，可替代 printf，编译最快 [[39]]
│       ├── core.h                       ← 兼容性头文件（已废弃），实际转发到 base.h [[93]]
│       ├── format.h                     ← 完整格式化 API：支持编译期检查、locale、用户自定义类型等 [[41]]
│       ├── format-inl.h                 ← format.h 的内联实现（header-only 模式必需）
│       ├── args.h                       ← 格式化参数处理与类型擦除实现
│       ├── chrono.h                     ← std::chrono 时间类型格式化支持
│       ├── color.h                      ← 终端 ANSI 颜色输出支持（如 fmt::print(fg(fmt::color::red), "...")）
│       ├── compile.h                    ← 编译期格式化（FMT_COMPILE 宏），提升运行时性能
│       ├── ostream.h                    ← 与 std::ostream 集成（如 fmt::print(std::cout, "...")）
│       ├── printf.h                     ← printf 风格 API（fmt::printf），便于迁移旧代码
│       ├── ranges.h                     ← C++20 ranges / std::tuple 格式化支持
│       ├── std.h                        ← 标准库类型扩展（如 std::filesystem::path, std::byte）[[66]]
│       ├── xchar.h                      ← 宽字符（wchar_t）和 Unicode 支持
│       └── os.h                         ← 操作系统相关功能（文件操作、系统错误码格式化等）
├── src/                                 ← 源文件实现（非 header-only 模式时编译为库）
│   ├── format.cc                        ← format.h 的非内联实现（核心格式化逻辑）
│   ├── os.cc                            ← os.h 的实现（文件 I/O、系统调用封装）
│   └── fmt.cc                           ← 辅助功能实现（如 Windows Unicode 路径处理）
├── test/                                ← 单元测试与集成测试代码（Google Test 框架）
│   ├── format-test.cc                   ← format.h 功能测试
│   ├── chrono-test.cc                   ← 时间格式化测试
│   ├── compile-test.cc                  ← 编译期格式化测试
│   ├── ranges-test.cc                   ← ranges/tuple 测试
│   ├── CMakeLists.txt                   ← 测试构建配置
│   └── ...                              ← 其他模块测试文件
├── doc/                                 ← 官方文档源文件（Sphinx + reStructuredText）
│   ├── api.rst                          ← API 参考文档
│   ├── usage.rst                        ← 使用指南
│   ├── index.rst                        ← 文档首页
│   └── conf.py                          ← Sphinx 配置
├── support/                             ← 构建辅助工具与脚本
│   ├── build.py                         ← Python 构建脚本
│   ├── cmake/                           ← CMake 模块（如 FindFMT.cmake）
│   └── ...
├── .github/                             ← GitHub Actions CI/CD 配置
│   ├── workflows/                       ← CI 流水线（测试、格式检查、发布）
│   └── ...
├── .clang-format                        ← 代码风格配置（LLVM 风格）
├── .clang-tidy                          ← 静态分析配置
├── .gitignore                           ← Git 忽略规则
├── LICENSE                              ← MIT 开源许可证
├── README.rst                           ← 项目简介与快速入门
└── ChangeLog.md                         ← 版本变更日志
```



# main.cpp

```cpp
#include <iostream>
#include "print.h"
#include <fmt/core.h>
#include <fmt/color.h>
using namespace std;

int main(int argc, char const *argv[]) {
  Print a;

  fmt::print(fg(fmt::color::crimson) | fmt::emphasis::bold,
             "Hello, {}!\n", "world");
  fmt::print(fg(fmt::color::floral_white) | bg(fmt::color::slate_gray) |
             fmt::emphasis::underline, "Olá, {}!\n", "Mundo");
  fmt::print(fg(fmt::color::steel_blue) | fmt::emphasis::italic,
             "你好{}!\n", "世界");
  fmt::print("{}Hello, {}!\n", "fmt: ", "world");

  a.print();
  return 0;
}
```







